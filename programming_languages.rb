def reformat_languages(languages)
  new_hash = {}
  languages.each do |style,language_hash|
    language.each do |language,type_hash|
      type.each do |type, descriptor| 
      new_hash[key] =  value 
      new_hash[key][:style] = [].push(style)
    end
  end
  new_hash
end